# import math
# from decimal import Decimal
# from maths.utils import get_digits

# N = 2
# base = pow(100,100)
# mod = pow(10,100)

# dsum = 0
# for n in range(2, N+1):
#     nsqr = int(math.sqrt(Decimal(n*base)))
#     nsqr = nsqr % mod
#     print(n, nsqr)
#     dsum += sum(get_digits(nsqr))

# dec = Decimal(math.sqrt(2))
# print(math.sqrt(2))
# print(dec.)
# print(dsum)