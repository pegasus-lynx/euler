# from common.paths import get_data_path


# keylog_path = get_data_path("0079_keylog.txt")
# keylog = [int(line) for line in keylog_path.read_text().splitlines() if line.strip()]

# print(sorted(list(set(keylog))))